user: joffrey-bion

secretBundles:
  digital-ocean:
    - DIGITALOCEAN_ACCESS_TOKEN
  dockerhub:
    - DOCKERHUB_USER
    - DOCKERHUB_TOKEN
  github-container-registry:
    - GH_CONTAINER_REGISTRY_TOKEN
  github-token:
    - GH_PAT
  gpg:
    - GPG_PASSWORD
    - GPG_SECRET_ASCII_ARMORED
  gradle-plugin-portal:
    - GRADLE_API_KEY
    - GRADLE_API_SECRET
  ossrh:
    - OSSRH_KEY
    - OSSRH_USER_TOKEN

repositories:
  checkstyle-config:
    bundles: [github-token, gpg, ossrh]
  chrome-devtools-kotlin:
    bundles: [github-token, gpg, ossrh]
  fx-gson:
    bundles: [github-token, gpg, ossrh]
  generics-explorer:
    bundles: [github-token, gpg, ossrh]
  gradle-github-changelog:
    bundles: [github-token, gradle-plugin-portal]
  gradle-kotlin-publish-plugin:
    bundles: [github-token, gradle-plugin-portal]
  hashcode-submit-gradle-plugin:
    bundles: [github-token, gradle-plugin-portal]
  hashcode-utils-kt:
    bundles: [github-token, gpg, ossrh]
  jackstomp:
    bundles: [github-token, gpg, ossrh]
  kotlin-blueprintjs:
    bundles: [github-token, gpg, ossrh]
  krossbow:
    bundles: [github-token, gpg, ossrh]
  livedoc:
    bundles: [github-token, gpg, ossrh]
  rol-automizer:
    bundles: [github-token, dockerhub, github-container-registry]
  seven-wonders:
    bundles: [github-token, dockerhub, github-container-registry, digital-ocean]
  simple-ocr:
    bundles: [github-token, gpg, ossrh]
