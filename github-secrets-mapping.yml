user: joffrey-bion

secretBundles:
  digital-ocean:
    - DIGITALOCEAN_ACCESS_TOKEN
  dockerhub:
    - DOCKERHUB_USER
    - DOCKERHUB_TOKEN
  github-container-registry:
    - GH_CONTAINER_REGISTRY_TOKEN
  github-token:
    - GH_PAT
  gpg:
    - GPG_PASSWORD
    - GPG_SECRET_ASCII_ARMORED
  gradle:
    - GRADLE_CACHE_ENCRYPTION_KEY
  gradle-plugin-portal:
    - GRADLE_API_KEY
    - GRADLE_API_SECRET
  ossrh:
    - OSSRH_TOKEN_USERNAME
    - OSSRH_TOKEN_PASSWORD
    # legacy names
    - OSSRH_KEY
    - OSSRH_USER_TOKEN
  maven-central-portal:
    - MAVEN_CENTRAL_TOKEN_USERNAME
    - MAVEN_CENTRAL_TOKEN_PASSWORD

repositories:
  accounting:
    bundles: [github-token, gradle, dockerhub, github-container-registry]
  checkstyle-config:
    bundles: [github-token, gradle, gpg, ossrh]
  chrome-devtools-kotlin:
    bundles: [github-token, gradle, gpg, ossrh]
  fx-gson:
    bundles: [github-token, gradle, gpg, ossrh]
  generics-explorer:
    bundles: [github-token, gradle, gpg, ossrh]
  gradle-github-changelog:
    bundles: [github-token, gradle, gradle-plugin-portal]
  gradle-kotlin-publish-plugin:
    bundles: [github-token, gradle, gradle-plugin-portal]
  har-parser:
    bundles: [github-token, gradle, gpg, ossrh]
  hashcode-submit-gradle-plugin:
    bundles: [github-token, gradle, gradle-plugin-portal]
  hashcode-utils-kt:
    bundles: [github-token, gradle, gpg, ossrh]
  idle-planet-miner-helper:
    bundles: [github-token, gradle]
  jackstomp:
    bundles: [github-token, gradle, gpg, ossrh]
  kotlin-blueprintjs:
    bundles: [github-token, gradle, gpg, ossrh]
  krossbow:
    bundles: [github-token, gradle, gpg, ossrh]
  leboncoin-booster:
    bundles: [github-token, gradle]
  livedoc:
    bundles: [github-token, gradle, gpg, ossrh]
  mc-mining-optimizer:
    bundles: [github-token, gradle]
  playground:
    bundles: [github-token, gradle]
  puzzle-solver:
    bundles: [github-token, gradle]
  rol-automizer:
    bundles: [github-token, gradle, dockerhub, github-container-registry]
  seven-wonders:
    bundles: [github-token, gradle, dockerhub, github-container-registry, digital-ocean]
  simple-ocr:
    bundles: [github-token, gradle, gpg, ossrh]
  socketio-kotlin:
    bundles: [github-token, gradle, gpg, ossrh]
  vipbelote:
    bundles: [github-token, gradle]
  youtube-to-mp3:
    bundles: [github-token, gradle]
